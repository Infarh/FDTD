<Window x:Class="FDTD2DLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:avalon="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:l="clr-namespace:FDTD2DLab"
        xmlns:svm="clr-namespace:FDTD2DLab.ViewModels.Shapes"
        xmlns:cvm="clr-namespace:FDTD2DLab.ViewModels.Source"
        xmlns:mvm="clr-namespace:FDTD2DLab.ViewModels.Material"
        xmlns:vm="clr-namespace:FDTD2DLab.ViewModels"
        xmlns:b="clr-namespace:FDTD2DLab.Infrastructure.Behaviors"
        xmlns:c="clr-namespace:FDTD2DLab.Infrastructure.Converters"
        xmlns:inf="clr-namespace:FDTD2DLab.Infrastructure"
        xmlns:sche="clr-namespace:FDTD2DLab.Infrastructure.Converters"
        DataContext="{Binding MainWindowModel, Source={StaticResource Locator}}"
        Title="{Binding Title}"
        Width="1100" Height="600">
    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+N" Command="{Binding CreateCommand}"/>
        <KeyBinding Gesture="Ctrl+O" Command="{Binding OpenCommand}"/>
        <KeyBinding Gesture="Ctrl+S" Command="{Binding SaveCommand}"/>
        <KeyBinding Gesture="Ctrl+Shift+S" Command="{Binding SaveAsCommand}"/>
        <KeyBinding Gesture="Ctrl+Q" Command="{Binding ExitCommand}"/>
        <KeyBinding Gesture="Ctrl+R" Command="{Binding Grid.AddRectShapeToGrid}"/>
        <KeyBinding Gesture="Ctrl+E" Command="{Binding Grid.AddEllipseShapeToGrid}"/>
        <KeyBinding Key="Delete" Command="{Binding Grid.DeletStructureCommand}"/>
    </Window.InputBindings>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <DockPanel>
        <Menu IsMainMenu="True">
            <MenuItem Header="Файл">
                <MenuItem Header="Создать" InputGestureText="Ctrl+N" Command="{Binding CreateCommand}"/>
                <MenuItem Header="Открыть" InputGestureText="Ctrl+O" Command="{Binding OpenCommand}"/>
                <MenuItem Header="Сохранить" InputGestureText="Ctrl+S" Command="{Binding SaveCommand}"/>
                <MenuItem Header="Сохранить как..." InputGestureText="Ctrl+Shift+S" Command="{Binding SaveAsCommand}"/>
                <Separator/>
                <MenuItem Header="Выход" InputGestureText="Ctrl+Q" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="Правка">
                <MenuItem Header="Отменить" InputGestureText="Ctrl+Z" Command="{Binding CreateCommand}"/>
                <MenuItem Header="Повторить" InputGestureText="Ctrl+Shift+Z" Command="{Binding OpenCommand}"/>
                <MenuItem Header="Настройки" InputGestureText="Ctrl+O" Command="{Binding OpenCommand}"/>
            </MenuItem>
            <MenuItem Header="Рисование">
                <MenuItem Header="Добавить объект">
                    <MenuItem Header="Прямоугольник" Command="{Binding AddRectShapeToGrid}"/>
                    <MenuItem Header="Элипс" Command="{Binding AddEllipseShapeToGrid}"/>
                </MenuItem>
                <MenuItem Header="Добавить источник">
                    <MenuItem Header="Точечный" Command="{Binding Grid.AddPointSourceCommand}"/>
                    <MenuItem Header="Плоской волны" Command="{Binding Grid.AddPlaneWaveSourceCommand}"/>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Параметры"/>
            <MenuItem Header="Экран"/>
            <MenuItem Header="Моделирование">
                <MenuItem Header="Настройки моделирования"/>
                <MenuItem Header="Быстрый запуск"/>
                <MenuItem Header="Запись GIF" Command="{Binding StartGifRecordingCommand}"/>
                <MenuItem Header="Реальное время" Command="{Binding StartRealTimeCommand}"/>
                <MenuItem Header="О создателях"/>
            </MenuItem>
            <MenuItem Header="Окно"/>
            <MenuItem Header="Справка">
                <MenuItem Header="Описание системы"/>
                <MenuItem Header="Система быстрых команд"/>
                <MenuItem Header="О создателях"/>
            </MenuItem>
        </Menu>

        <StatusBar>
            <StatusBarItem DockPanel.Dock="Right">
                <ResizeGrip/>
            </StatusBarItem>
            <StatusBarItem Content="{Binding Status}"/>
        </StatusBar>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button fa:Awesome.Content="Regular_File" Command="{Binding CreateCommand}" ToolTip="Создать проект"/>
                <Button fa:Awesome.Content="Regular_FolderOpen" Command="{Binding OpenCommand}" ToolTip="Открыть проект"/>
                <Button fa:Awesome.Content="Regular_Save" Command="{Binding SaveCommand}" ToolTip="Сохранить проект"/>
                <Button fa:Awesome.Content="Solid_Save" Command="{Binding SaveAsCommand}" ToolTip="Сохранить проект как..."/>
                <Separator/>
                <Button fa:Awesome.Content="Solid_Cogs" ToolTip="Настройки"/>
                <Button fa:Awesome.Content="Solid_BorderAll" ToolTip="Настройки"/>
                <Separator/>
                <Button fa:Awesome.Content="Solid_Play" ToolTip="Моделирование"/>
            </ToolBar>
        </ToolBarTray>
        <TabControl>
            <TabItem Header="Пространство">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="120"/>
                    </Grid.RowDefinitions>
                    <GroupBox Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"
                              BorderThickness="0"
                              DataContext="{Binding Grid}">

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Expander Header="Сетка" IsExpanded="True">
                                    <Grid Margin="5">
                                        <Grid.Resources>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <Trigger Property="Grid.Column" Value="0">
                                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="Margin" Value="0,0,3,0"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Margin" Value="0,1.5,1,1.5"/>
                                            </Style>
                                            <Style TargetType="ComboBox">
                                                <Setter Property="Margin" Value="0,1.5,0,1.5"/>
                                            </Style>
                                        </Grid.Resources>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="44"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Column="0" Grid.Row="0" Text="Nx:" HorizontalAlignment="Right"/>
                                        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Nx}"/>

                                        <TextBlock Grid.Column="0" Grid.Row="1" Text="Ny:" HorizontalAlignment="Right"/>
                                        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Ny}"/>

                                        <TextBlock Grid.Column="0" Grid.Row="2" Text="dx:" HorizontalAlignment="Right"/>
                                        <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding dx}"/>
                                        <ComboBox Grid.Column="2" Grid.Row="2"
                                          ItemsSource="{Binding SpaceUnits}" 
                                          SelectedItem="{Binding SpaceUnit}">
                                            <ComboBox.ToolTip>
                                                <TextBlock Text="{Binding SpaceUnitValue, StringFormat={}{0} м}"/>
                                            </ComboBox.ToolTip>
                                        </ComboBox>

                                        <TextBlock Grid.Column="0" Grid.Row="3" Text="dy:" HorizontalAlignment="Right"/>
                                        <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding dy}"/>
                                        <ComboBox Grid.Column="2" Grid.Row="3"
                                          ItemsSource="{Binding SpaceUnits}" 
                                          SelectedItem="{Binding SpaceUnit}">
                                            <ComboBox.ToolTip>
                                                <TextBlock Text="{Binding SpaceUnitValue, StringFormat={}{0} м}"/>
                                            </ComboBox.ToolTip>
                                        </ComboBox>

                                        <TextBlock Grid.Column="0" Grid.Row="4" Text="Lx:" HorizontalAlignment="Right"/>
                                        <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding Lx}"/>
                                        <TextBlock Grid.Column="2" Grid.Row="4" Text="{Binding SpaceUnit}" Margin="6,0,0,0"/>

                                        <TextBlock Grid.Column="0" Grid.Row="5" Text="Ly:" HorizontalAlignment="Right"/>
                                        <TextBlock Grid.Column="1" Grid.Row="5" Text="{Binding Ly}"/>
                                        <TextBlock Grid.Column="2" Grid.Row="5" Text="{Binding SpaceUnit}" Margin="6,0,0,0"/>

                                    </Grid>
                                </Expander>
                                <Expander Header="Фигуры" IsExpanded="True" Padding="3">
                                    <ListBox ItemsSource="{Binding Shapes}"
                                             SelectedItem="{Binding SelectedShape}">
                                        <ListBox.Resources>
                                            <DataTemplate DataType="{x:Type svm:RectViewModel}">
                                                <TextBlock Text="Прямоугольник"/>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type svm:EllipseViewModel}">
                                                <TextBlock Text="Эллипс"/>
                                            </DataTemplate>
                                        </ListBox.Resources>
                                    </ListBox>
                                </Expander>
                                <Expander Header="Источники" IsExpanded="True" Padding="3">
                                    <StackPanel>
                                        <ListBox ItemsSource="{Binding Sources}"
                                                    SelectedItem="{Binding SelectedSource}"
                                                    DisplayMemberPath="Name">
                                            <ListBox.Resources>
                                                <DataTemplate DataType="{x:Type cvm:PointSourceViewModel}">
                                                    <TextBlock Text="Точечный"/>
                                                </DataTemplate>
                                                <DataTemplate DataType="{x:Type cvm:PlaneWaveSourceViewModel}">
                                                    <TextBlock Text="Плоский"/>
                                                </DataTemplate>
                                            </ListBox.Resources>
                                        </ListBox>
                                        <!--<StackPanel Orientation="Horizontal" Margin="0,5">
                                            <Button Content="Точечный" Command="{Binding AddPointSourceCommand}" Margin="2"/>
                                            <Button Content="Плоская волна" Command="{Binding AddPlaneWaveSourceCommand}" Margin="2"/>
                                        </StackPanel>-->
                                    </StackPanel>
                                </Expander>
                                <Expander Header="Материалы" IsExpanded="True" Padding="3">
                                    <StackPanel>
                                        <ListBox ItemsSource="{Binding Materials}" 
                                                SelectedItem="{Binding SelectedMaterial}"
                                                DisplayMemberPath="Name"
                                                Background="Transparent">
                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Setter Property="b:DragDropBehavior.CanDrag" Value="True"/>
                                                </Style>
                                            </ListBox.ItemContainerStyle>
                                        </ListBox>
                                        <Button Content="Добавить материал" Command="{Binding AddMaterialCommand}" Margin="2"/>
                                    </StackPanel>
                                </Expander>
                                <Expander Header="Графики" IsExpanded="True" Padding="3">
                                    <ListBox ItemsSource="{Binding Shapes}"
                                            SelectedItem="{Binding SelectedShape}">
                                        <ListBox.Resources>
                                            <DataTemplate DataType="{x:Type svm:RectViewModel}">
                                                <TextBlock Text="Прямоугольник"/>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type svm:EllipseViewModel}">
                                                <TextBlock Text="Эллипс"/>
                                            </DataTemplate>
                                        </ListBox.Resources>
                                    </ListBox>
                                </Expander>
                            </StackPanel>
                        </ScrollViewer>
                    </GroupBox>
                    <GroupBox Grid.Column="1" Grid.Row="1" BorderThickness="0" Margin="3">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock>
                                    Cell
                                    <TextBlock Text="{Binding Grid.MousePositionCell.X, StringFormat=x:{0:f0}}" Width="50"/>
                                    (<TextBlock Text="{Binding Grid.MousePosition.X, StringFormat={}{0:0.##}}"/>
                                    <TextBlock Text="{Binding Grid.SpaceUnit}"/>)
                                </TextBlock>
                                <TextBlock>
                                    Cell
                                    <TextBlock Text="{Binding Grid.MousePositionCell.Y, StringFormat=y:{0:f0}}" Width="50"/>
                                    (<TextBlock Text="{Binding Grid.MousePosition.Y, StringFormat={}{0:0.##}}"/>
                                    <TextBlock Text="{Binding Grid.SpaceUnit}"/>)
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Grid.Column="1" DataContext="{Binding Grid.SelectedShape}">
                                <DockPanel>
                                    <TextBlock Text="{Binding X, StringFormat=x:{0:f2}}" Margin="0,0,3,0" Width="40"/>
                                    <Slider Value="{Binding X}" SmallChange="1"
                                            Maximum="{Binding DataContext.Grid.Lx, RelativeSource={RelativeSource AncestorType=GroupBox}}"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Y, StringFormat=y:{0:f2}}" Margin="0,0,3,0" Width="40"/>
                                    <Slider Value="{Binding Y}" SmallChange="1"
                                            Maximum="{Binding DataContext.Grid.Ly, RelativeSource={RelativeSource AncestorType=GroupBox}}"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Width, StringFormat=w:{0:f2}}" Margin="0,0,3,0" Width="40"/>
                                    <Slider Value="{Binding Width}" SmallChange="1"
                                            Maximum="{Binding DataContext.Grid.Lx, RelativeSource={RelativeSource AncestorType=GroupBox}}"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Height, StringFormat=h:{0:f2}}" Margin="0,0,3,0" Width="40"/>
                                    <Slider Value="{Binding Height}" SmallChange="1"
                                            Maximum="{Binding DataContext.Grid.Lx, RelativeSource={RelativeSource AncestorType=GroupBox}}" ValueChanged="Slider_ValueChanged"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Angle, StringFormat=a:{0:f2}}" Margin="0,0,3,0" Width="40"/>
                                    <Slider Value="{Binding Angle}" SmallChange="1" Minimum="-360" Maximum="360"/>
                                </DockPanel>
                            </StackPanel>
                        </Grid>
                    </GroupBox>

                    <DockPanel Grid.Column="1" Grid.Row="0">
                        <ToolBarTray DockPanel.Dock="Top" Background="White" Grid.Column="0">
                            <ToolBar Background="White">
                                <Button fa:Awesome.Content="Solid_Cut" ToolTip="Вырезать"/>
                                <Button fa:Awesome.Content="Solid_Copy" ToolTip="Копировать"/>
                                <Button fa:Awesome.Content="Solid_Paste" ToolTip="Вставить"/>
                                <Separator/>
                                <TextBlock VerticalAlignment="Center" Text="Объекты:"/>
                                <Button Width="16.5" Height="19" Command="{Binding Grid.AddRectShapeToGrid}" ToolTip="Прямоугольник" >
                                    <Rectangle Fill="Blue" Width="8" Height="13" Stroke="DarkBlue" StrokeThickness="1"/>
                                </Button>
                                <Button Width="16.5" Height="19" Command="{Binding Grid.AddEllipseShapeToGrid}" ToolTip="Эллипс">
                                    <Ellipse Fill="Blue" Width="8" Height="13" Stroke="DarkBlue" StrokeThickness="1"/>
                                </Button>
                                <Separator/>
                                <TextBlock VerticalAlignment="Center" Text="Источники:"/>
                                <Button Width="16.5" Height="19" ToolTip="Точечный" Command="{Binding Grid.AddPointSourceCommand}">
                                    <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1"/>
                                </Button>
                                <Button Width="16.5" Height="19" ToolTip="Линия">
                                    <Rectangle Fill="Red" Width="2" Height="13" />
                                </Button>
                                <Button Width="16.5" Height="19" ToolTip="Плоская падающая волна" Command="{Binding Grid.AddPlaneWaveSourceCommand}">
                                    <Rectangle Width="9" Height="13" >
                                        <Rectangle.Fill>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="Transparent"/>
                                                <GradientStop Color="Red" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                </Button>
                                <Separator/>
                                <TextBlock VerticalAlignment="Center" Text="Зонд:"/>
                                <Button Width="16.5" Height="19" ToolTip="Точечный">
                                    <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1"/>
                                </Button>
                                <Separator/>
                                <TextBlock VerticalAlignment="Center" Text="Моделирование:"/>
                                <Button Width="16.5" Height="19" ToolTip="Точечный">
                                    <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1"/>
                                </Button>
                                <Button Width="16.5" Height="19" ToolTip="Точечный">
                                    <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1"/>
                                </Button>
                                <Button Width="16.5" Height="19" ToolTip="Точечный">
                                    <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1"/>
                                </Button>
                                <Button Width="16.5" Height="19" ToolTip="Точечный">
                                    <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1"/>
                                </Button>
                            </ToolBar>
                        </ToolBarTray>
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>


                            <Border ClipToBounds="True" BorderThickness="1" BorderBrush="Black" Padding="1" Grid.Column="0">
                                <Grid>
                                    <Image Source="{Binding FieldBitmap}" 
                                        Stretch="Fill" 
                                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                                        Panel.ZIndex="0" 
                                        Opacity="0.7"/>
                                    
                                    <ItemsControl ItemsSource="{Binding Grid.GridX}">
                                        <ItemsControl.Resources>
                                            <c:Div x:Key="DivX" Parameter="{Binding Grid.Nx}"/>
                                        </ItemsControl.Resources>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderThickness="0,0,0.25,0" BorderBrush="Gray"
                                                Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource DivX}}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style TargetType="ContentPresenter">
                                                <Setter Property="inf:RelPos.MouseButtonDownCommand" Value="{Binding DataContext.Grid.SetShapeCommandCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                    </ItemsControl>

                                    <ItemsControl ItemsSource="{Binding Grid.GridY}">
                                        <ItemsControl.Resources>
                                            <c:Div x:Key="DivY" Parameter="{Binding Grid.Ny}"/>
                                        </ItemsControl.Resources>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Vertical"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderThickness="0,0,0,0.25" BorderBrush="Gray"
                                                Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource DivY}}"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>

                                    </ItemsControl>

                                    <ItemsControl ItemsSource="{Binding Grid.Shapes}" Background="Transparent">
                                        <i:Interaction.Behaviors>
                                            <b:MouseMove PositionRelative="{Binding Grid.GridMousePosition, Mode=OneWayToSource}"/>
                                        </i:Interaction.Behaviors>
                                        <ItemsControl.Resources>
                                            <Divide x:Key="Div-2" Parameter="-2"></Divide>
                                            <Divide x:Key="Div+2" Parameter="+2"></Divide>
                                            <DataTemplate DataType="{x:Type svm:EllipseViewModel}">
                                                <Grid Background="Transparent">
                                                    <i:Interaction.Behaviors>
                                                        <b:MouseWheelIncrement Value="{Binding Angle}" Ratio="-0.01"/>
                                                    </i:Interaction.Behaviors>
                                                    <Ellipse Fill="Blue"/>
                                                    <Border>
                                                        
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="BorderBrush" Value="Red"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Setter Property="Margin" Value="-2"/>
                                                                <Setter Property="Visibility" Value="Hidden"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                    </Border>
                                                </Grid>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type svm:RectViewModel}">
                                                <Grid Background="Transparent">
                                                    <i:Interaction.Behaviors>
                                                        <b:MouseWheelIncrement Value="{Binding Angle}" Ratio="-0.01"/>
                                                    </i:Interaction.Behaviors>
                                                    <Rectangle Fill="DarkSlateGray"/>
                                                    <Border>
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="BorderBrush" Value="Red"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Setter Property="Margin" Value="-2"/>
                                                                <Setter Property="Visibility" Value="Hidden"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                    </Border>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.Resources>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style TargetType="ContentPresenter">
                                                <Setter Property="b:DragDropBehavior.CanDrop" Value="True"/>
                                                <Setter Property="inf:RelPos.Binding" Value="True"/>
                                                <Setter Property="inf:RelPos.ContainerWidth" Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.ContainerHeight" Value="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.MaxX" Value="{Binding DataContext.Grid.Lx, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.MaxY" Value="{Binding DataContext.Grid.Ly, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.X" Value="{Binding X}"/>
                                                <Setter Property="inf:RelPos.Y" Value="{Binding Y}"/>
                                                <Setter Property="inf:RelPos.ValueWidth" Value="{Binding Width}"/>
                                                <Setter Property="inf:RelPos.ValueHeight" Value="{Binding Height}"/>
                                                <Setter Property="inf:RelPos.MouseButtonDownCommand" Value="{Binding DataContext.Grid.SetShapeCommandCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                                <!--<Setter Property="inf:RelPos.MouseButtonUpCommand" Value="{Binding DataContext.Grid.UnSetShapeCommandCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>-->
                                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <TransformGroup>
                                                            <RotateTransform Angle="{Binding Angle}"/>
                                                            <TranslateTransform X="{Binding Width, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource Div-2}}"
                                                                        Y="{Binding Height, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource Div+2}}"/>
                                                        </TransformGroup>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>

                                    <ItemsControl ItemsSource="{Binding Grid.Sources}" Panel.ZIndex="2">
                                        <ItemsControl.Resources>
                                            <!-- Шаблон для точечного источника -->
                                            <DataTemplate DataType="{x:Type cvm:PointSourceViewModel}">
                                                <Ellipse Fill="Red" Width="5" Height="5" Stroke="DarkRed" StrokeThickness="1" 
                                                ToolTip="{Binding Name}"/>
                                            </DataTemplate>

                                            <!-- Шаблон для плоской волны -->
                                            <DataTemplate DataType="{x:Type cvm:PlaneWaveSourceViewModel}">
                                                <Rectangle Fill="Red" Stroke="DarkRed" StrokeThickness="1" 
                                                    ToolTip="{Binding Name}">
                                                    <Rectangle.Style>
                                                        <Style TargetType="Rectangle">
                                                            <Setter Property="inf:RelPos.X" Value="{Binding Start}"/>
                                                            <Setter Property="inf:RelPos.Y" Value="{Binding Position}"/>
                                                            <Setter Property="inf:RelPos.ValueWidth" Value="{Binding End, Converter={StaticResource Subtract}, ConverterParameter={Binding Start}}"/>
                                                            <Setter Property="inf:RelPos.ValueHeight" Value="0.001"/>
                                                            <!-- малая толщина -->
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsHorizontal}" Value="False">
                                                                    <Setter Property="inf:RelPos.X" Value="{Binding Position}"/>
                                                                    <Setter Property="inf:RelPos.Y" Value="{Binding Start}"/>
                                                                    <Setter Property="inf:RelPos.ValueWidth" Value="0.001"/>
                                                                    <Setter Property="inf:RelPos.ValueHeight" Value="{Binding End, Converter={StaticResource Subtract}, ConverterParameter={Binding Start}}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Rectangle.Style>
                                                </Rectangle>
                                            </DataTemplate>
                                        </ItemsControl.Resources>

                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Canvas/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemContainerStyle>
                                            <Style TargetType="ContentPresenter">
                                                <!-- Привязка команды выбора источника -->
                                                <Setter Property="inf:RelPos.MouseButtonDownCommand" 
                                                    Value="{Binding DataContext.Grid.SelectSourceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                                <!-- Параметры для позиционирования RelPos -->
                                                <Setter Property="inf:RelPos.ContainerWidth" 
                                                    Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.ContainerHeight" 
                                                    Value="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.MaxX" 
                                                    Value="{Binding DataContext.Grid.Lx, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                                <Setter Property="inf:RelPos.MaxY" 
                                                    Value="{Binding DataContext.Grid.Ly, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                    </ItemsControl>

                                </Grid>
                            </Border>

                            <ScrollViewer Grid.Column="1" DataContext="{Binding Grid}" VerticalScrollBarVisibility="Visible">
                                <StackPanel >
                                    <StackPanel>
                                        <ContentControl Content="{Binding SelectedProperty}">
                                        <ContentControl.Resources>
                                            <DataTemplate DataType="{x:Type svm:EllipseViewModel}">
                                                <StackPanel Margin="1,2,1,1">
                                                    <TextBlock Text="ЭЛЛИПС" TextAlignment="Center" FontSize="15" FontFamily="Segoe UI Semibold"/>
                                                    <StackPanel  Grid.IsSharedSizeScope="True">
                                                        <StackPanel.Resources>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="Margin" Value="0,1"></Setter>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="TextBox">
                                                                            <Grid>
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Header"/>
                                                                                    <ColumnDefinition/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <ContentPresenter Grid.Column="0" Content="{TemplateBinding Tag}"
                                                                                    Margin="0,0,3,0"
                                                                                    HorizontalAlignment="Right" TextBlock.FontWeight="Bold"/>
                                                                                <Border Grid.Column="1" x:Name="border"
                                                                                        Background="{TemplateBinding Background}"
                                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                                        SnapsToDevicePixels="True">
                                                                                    <ScrollViewer x:Name="PART_ContentHost"
                                                                                         Focusable="False"
                                                                                         HorizontalScrollBarVisibility="Hidden"
                                                                                         VerticalScrollBarVisibility="Hidden"/>
                                                                                </Border>
                                                                            </Grid>
                                                                            <ControlTemplate.Triggers>
                                                                                <Trigger Property="IsEnabled" Value="False">
                                                                                    <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                                                                                </Trigger>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter TargetName="border" Property="BorderBrush" Value="Blue"/>
                                                                                </Trigger>
                                                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                                                    <Setter TargetName="border" Property="BorderBrush" Value="DarkBlue"/>
                                                                                </Trigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Style>
                                                        </StackPanel.Resources>

                                                        <TextBlock Text="Горизонтальное положение" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="x:" Text="{Binding X}" ToolTip="Горизонтальное положение"/>
                                                        <Rectangle Height="1" Fill="Gray" Margin="2,5,2,2"/>
                                                        <TextBlock Text="Расположение по y" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="y:" Text="{Binding Y}" ToolTip="Вертикальное положение"/>
                                                        <Rectangle Height="1" Fill="Gray" Margin="2,5,2,2"/>
                                                        <TextBlock Text="Горизонтальный размер" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="w:" Text="{Binding Width}" ToolTip="Ширина"/>
                                                        <Rectangle Height="1" Fill="Gray" Margin="2,5,2,2"/>
                                                        <TextBlock Text="Вертикальный размер" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="h:" Text="{Binding Height}" ToolTip="Высота"/>
                                                        <TextBlock Text="Угол поворота" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="ang:" Text="{Binding Angle}" ToolTip="Угол поворота"/>
                                                        <Rectangle Height="2" Fill="DarkCyan" Margin="2,5,2,2"/>
                                                        
                                                        </StackPanel>
                                                </StackPanel>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type svm:RectViewModel}">
                                                <StackPanel Margin="1,2,1,1">
                                                    <TextBlock Text="ПРЯМОУГОЛЬНИК" TextAlignment="Center" FontSize="15" FontFamily="Segoe UI Semibold"/>
                                                    <StackPanel  Grid.IsSharedSizeScope="True">
                                                        <StackPanel.Resources>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="Margin" Value="0,1"></Setter>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="TextBox">
                                                                            <Grid>
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Header"/>
                                                                                    <ColumnDefinition/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <ContentPresenter Grid.Column="0" Content="{TemplateBinding Tag}"
                                                                                Margin="0,0,3,0"
                                                                                HorizontalAlignment="Right" TextBlock.FontWeight="Bold"/>
                                                                                <Border Grid.Column="1" x:Name="border"
                                                                                Background="{TemplateBinding Background}"
                                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                                SnapsToDevicePixels="True">
                                                                                <ScrollViewer x:Name="PART_ContentHost"
                                                                                Focusable="False"
                                                                                HorizontalScrollBarVisibility="Hidden"
                                                                                VerticalScrollBarVisibility="Hidden"/>
                                                                                </Border>
                                                                            </Grid>
                                                                            <ControlTemplate.Triggers>
                                                                                <Trigger Property="IsEnabled" Value="False">
                                                                                    <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                                                                                </Trigger>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter TargetName="border" Property="BorderBrush" Value="Blue"/>
                                                                                </Trigger>
                                                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                                                    <Setter TargetName="border" Property="BorderBrush" Value="DarkBlue"/>
                                                                                </Trigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Style>
                                                        </StackPanel.Resources>

                                                        <TextBlock Text="Горизонтальное положение" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="x:" Text="{Binding X}" ToolTip="Горизонтальное положение"/>
                                                        <Rectangle Height="1" Fill="Gray" Margin="2,5,2,2"/>
                                                        <TextBlock Text="Расположение по y" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="y:" Text="{Binding Y}" ToolTip="Вертикальное положение"/>
                                                        <Rectangle Height="1" Fill="Gray" Margin="2,5,2,2"/>
                                                        <TextBlock Text="Горизонтальный размер" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="w:" Text="{Binding Width}" ToolTip="Ширина"/>
                                                        <Rectangle Height="1" Fill="Gray" Margin="2,5,2,2"/>
                                                        <TextBlock Text="Вертикальный размер" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="h:" Text="{Binding Height}" ToolTip="Высота"/>
                                                        <TextBlock Text="Угол поворота" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="ang:" Text="{Binding Angle}" ToolTip="Угол поворота"/>
                                                        <Rectangle Height="2" Fill="DarkCyan" Margin="2,5,2,2"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type vm:GridViewModel}">
                                                <StackPanel>
                                                    <TextBlock Text="сетка"/>
                                                </StackPanel>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type cvm:PointSourceViewModel}">
                                                <StackPanel>
                                                    <TextBlock Text="Точечный источник" FontWeight="Bold"/>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition/>
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="X:" Margin="2"/>
                                                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding X}"/>
                                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Y:" Margin="2"/>
                                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Y}"/>
                                                        <TextBlock Text="Угол поворота" TextAlignment="Left" Padding="30,1,1,1"/>
                                                        <TextBox Tag="ang:" Text="{Binding Angle}" ToolTip="Угол поворота"/>
                                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Сигнал:" Margin="2"/>
                                                        <ComboBox Grid.Row="2" Grid.Column="1" SelectedItem="{Binding SignalType}"
                                                                ItemsSource="{Binding Source={StaticResource SignalTypes}}"/>
                                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Амплитуда:" Margin="2"/>
                                                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Amplitude}"/>
                                                        <!-- Параметры для Gaussian -->
                                                        <TextBlock Grid.Row="4" Grid.Column="0" Text="T0 (с):" Margin="2"/>
                                                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding T0}"/>
                                                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Tau (с):" Margin="2"/>
                                                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Tau}"/>
                                                        <!-- Параметры для Sine (видимость по условию) -->
                                                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Частота (Гц):" Margin="2"/>
                                                        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Frequency}"/>
                                                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Фаза (рад):" Margin="2"/>
                                                        <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Phase}"/>
                                                    </Grid>
                                                </StackPanel>
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type cvm:PlaneWaveSourceViewModel}">
                                                    <StackPanel>
                                                        <TextBlock Text="Источник плоской волны" FontWeight="Bold"/>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition/>
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="X:" Margin="2"/>
                                                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding X}"/>
                                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Y:" Margin="2"/>
                                                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Y}"/>
                                                            <TextBlock Text="Угол поворота" TextAlignment="Left" Padding="30,1,1,1"/>
                                                            <TextBox Tag="ang:" Text="{Binding Angle}" ToolTip="Угол поворота"/>
                                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Сигнал:" Margin="2"/>
                                                            <ComboBox Grid.Row="2" Grid.Column="1" SelectedItem="{Binding SignalType}"
                                                             ItemsSource="{Binding Source={StaticResource SignalTypes}}"/>
                                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Амплитуда:" Margin="2"/>
                                                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Amplitude}"/>
                                                            <!-- Параметры для Gaussian -->
                                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="T0 (с):" Margin="2"/>
                                                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding T0}"/>
                                                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Tau (с):" Margin="2"/>
                                                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Tau}"/>
                                                            <!-- Параметры для Sine (видимость по условию) -->
                                                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Частота (Гц):" Margin="2"/>
                                                            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Frequency}"/>
                                                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Фаза (рад):" Margin="2"/>
                                                            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Phase}"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ContentControl.Resources>
                                    </ContentControl>
                                        <StackPanel.Resources>

                                        </StackPanel.Resources>
                                    </StackPanel>
                                    <!--<StackPanel Name="Material_shape">
                                        <ContentControl Content="{Binding SelectedProperty}">
                                            <ContentControl.Resources>
                                                <DataTemplate DataType="{x:Type svm:ShapeViewModel}">
                                                    <StackPanel>
                                                        <ComboBox ItemsSource="{Binding DataContext.Grid.Materials, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            SelectedItem="{Binding AppliedMaterial}"
                                                            DisplayMemberPath="Name"
                                                            Margin="2"/>
                                                        <TextBlock Text="Материал" FontWeight="Bold" Margin="0,5"/>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition/>
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:" Margin="2"/>
                                                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}"/>
                                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="ε:" Margin="2"/>
                                                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Eps}"/>
                                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="μ:" Margin="2"/>
                                                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Mu}"/>
                                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="σ:" Margin="2"/>
                                                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Sigma}"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ContentControl.Resources>
                                        </ContentControl>
                                    </StackPanel>-->
                                    <StackPanel>
                                        <ContentControl Content="{Binding SelectedMaterial}">
                                            <ContentControl.Resources>
                                                <DataTemplate DataType="{x:Type mvm:MaterialViewModel}">
                                                    <StackPanel>
                                                        <ComboBox ItemsSource="{Binding DataContext.Grid.Materials, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            SelectedItem="{Binding AppliedMaterial}"
                                                            DisplayMemberPath="Name"
                                                            Margin="2"/>
                                                        <TextBlock Text="Материал" FontWeight="Bold" Margin="0,5"/>
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition/>
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:" Margin="2"/>
                                                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}"/>
                                                            <TextBlock Grid.Row="1" Grid.Column="0" Text="ε:" Margin="2"/>
                                                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Eps}"/>
                                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="μ:" Margin="2"/>
                                                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Mu}"/>
                                                            <TextBlock Grid.Row="3" Grid.Column="0" Text="σ:" Margin="2"/>
                                                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Sigma}"/>
                                                        </Grid>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ContentControl.Resources>
                                        </ContentControl>
                                    </StackPanel>
                                </StackPanel>

                            </ScrollViewer>
                        </Grid>
                    </DockPanel>
                    <DockPanel >

                    </DockPanel>
                </Grid>

            </TabItem>
            <TabItem Header="Поле">
                <Grid>
                    <Image Source="{Binding FieldBitmap}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Text="Моделирование не запущено" Visibility="{Binding IsSimulating, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=inverse}"/>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
