<UserControl x:Class="FDTD2DLab.Views.FieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кадр GIF -->
        <Image Source="{Binding CurrentFrame}" Stretch="Uniform" />

        <!-- Элементы управления -->
        <StackPanel Grid.Row="1" Margin="5">
            <Slider Value="{Binding Progress, Mode=OneWay}" Minimum="0" Maximum="1"
        IsMoveToPointEnabled="True"
        ValueChanged="Slider_OnValueChanged"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="▶" Command="{Binding PlayCommand}" Margin="2" Width="30"/>
                <Button Content="⏸" Command="{Binding PauseCommand}" Margin="2" Width="30"/>
                <Button Content="⏹" Command="{Binding StopCommand}" Margin="2" Width="30"/>
                <TextBlock Text="{Binding CurrentFrameIndex, StringFormat='Кадр {0}'}" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
